===排序(必须先排序)
==使用添加元素的方法的话,每次都必须从0开始,可以使用一个used表来记录元素有没有用过
=如果元素没有重复
!used[i]
=如果元素有重复
对于一列相同数量的元素来说,只有该元素前面的元素被用过了,该元素才能被使用.

==交换法
=如果元素没有重复
每次交换都要把自己继承着向下进行
=如果元素有重复
判断该元素是否是数列中的相同元素的最后一个,如果是,那么就可以进行交换,否则不交换.